// Byte-identical duplicate of the core icon (ASMA-8134) — same artwork, viewBox and `1rem` default
// size. Re-exported to keep the `src/table/shared-components/PinIcon` import path stable.
export { PinIcon } from 'src/components/icons/pin-icon'
