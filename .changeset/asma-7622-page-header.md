---
'asma-ui-core': minor
---

ASMA-7622: new shared `PageHeader` component — one container-width-adaptive page
header for all systems (title with 2-line clamp, back/menu leading control or
custom `leadingSlot`, adaptive actions with badge counts and "More" overflow,
subtitle/status slots, search mode, loading skeleton, sticky variant). Toolbar
translations gain `back`/`menu`/`close` strings (en/no).
