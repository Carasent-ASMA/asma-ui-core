---
'asma-ui-core': minor
---

Add `usePopupState` hook (+ `PopupState`, `bindTrigger`, `bindPopover`, `bindPopper`) and a headless `Listbox` compound (`Listbox.Button`/`.Options`/`.Option`) — dependency-free replacements for `material-ui-popup-state` and `@headlessui/react`'s Listbox, so consuming apps can drop those libraries.
